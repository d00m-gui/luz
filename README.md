# luz

A simple library for manage CSS via a config file, that generates Design Tokens on a standarized reseted HTML/CSS, dynamically loads on Vanilla JS via `CSSStyleSheet()` and in React v19 with its new `<stylesheet>` management.
