---
import Layout from "../layouts/Layout.astro";
import { luz } from "../../../src/luz";
import { config } from "../../luz.config.ts";

const { tokens } = luz(config);

const WEIGHTS = [50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950];
const SHADE_SCALES = ["primary", "secondary", "neutral"];
const WHEEL_HUES = [
  "red",
  "orange",
  "copper",
  "yellow",
  "green",
  "emerald",
  "teal",
  "cyan",
  "blue",
  "sky",
];

const TEXT_SCALE_NAMES = ["xs", "sm", "base", "lg", "xl", "2xl", "3xl"];

function swatchStyle(name: string, weight: number) {
  const raw = tokens.colors[`${name}-${weight}`];
  const lightnessMatch = raw?.match(/oklch\(from \S+ ([\d.]+)%/);
  const lightness = lightnessMatch
    ? Number(lightnessMatch[1])
    : weight <= 400
      ? 80
      : 20;
  const onColor = lightness >= 50 ? `on-${name}-inverse` : `on-${name}`;
  return `background: var(--${name}-${weight}); color: var(--${onColor})`;
}
---

<Layout
  title="Luz"
  activeId="luz-defaults"
  breadcrumb="Tokens"
  contentClass="luz-defaults"
>
  <h2>Tokens</h2>
  <div class="docs-sections">
    <section class="card">
      {
        SHADE_SCALES.map((name) => (
          <div class="swatch-row">
            <div class="swatch-row-label">{name}</div>
            <div class="swatch-list">
              {WEIGHTS.map((weight) => (
                <div class="swatch" style={swatchStyle(name, weight)}>
                  <span>{weight}</span>
                </div>
              ))}
            </div>
          </div>
        ))
      }

      {
        WHEEL_HUES.map((name) => (
          <div class="swatch-row">
            <div class="swatch-row-label">{name}</div>
            <div class="swatch-list">
              {WEIGHTS.map((weight) => (
                <div class="swatch" style={swatchStyle(name, weight)}>
                  <span>{weight}</span>
                </div>
              ))}
            </div>
          </div>
        ))
      }
    </section>

    <section class="card">
      <h2 class="card-title">Sizes</h2>

      <div class="text-scale">
        {
          TEXT_SCALE_NAMES.map((name) => (
            <div class="text-scale-step">
              <span style={`font-size: var(--font-size-${name})`}>Aa</span>
              <code>--font-size-{name}</code>
            </div>
          ))
        }
      </div>

      <div class="size-scale">
        {
          ["border-radius", "border-width", "spacing"].map((name) => (
            <div class="size-step">
              <div
                class="size-swatch size-swatch-flat"
                style={`width: var(--${name}); height: calc(var(--size-unit) * 6)`}
              />
              <code>--{name}</code>
            </div>
          ))
        }
      </div>
    </section>

    <section class="card">
      <h2 class="card-title">Typography</h2>
      <div class="html fluid">
        <h1>Make it easy to read</h1>
        <h2>Make it easy to read</h2>
        <h3>Make it easy to read</h3>
        <h4>Make it easy to read</h4>
        <h5>Make it easy to read</h5>
        <h6>Make it easy to read</h6>
        <h1>Heading 1</h1>
        <p>
          Quisque nec mauris sit amet mi <strong>strong</strong> dictum varius. Aenean
          ultricies dui et dictum <a href="#">link</a> molestie. Maecenas in augue
          ex. Nam diam eros, gravida id euismod elementum, euismod quis ante. Nunc{
            " "
          }
          <a href="#" class="secondary">secondary link</a> tincidunt iaculis tempus.
          Phasellus sed dapibus enim. Pellentesque auctor orci non ligula aliquam,
          sed rhoncus nisi malesuada. Vestibulum tincidunt et est ut fringilla. Suspendisse
          ut dolor vitae sem feugiat porttitor sit amet ut nisl. Integer vitae ante
          <em>emphasis</em> odio. Quisque congue maximus metus, vel facilisis arcu
          blandit ac. Vestibulum nec urna laoreet, vestibulum neque sagittis, blandit
          nibh. Nullam a enim sit amet tellus commodo dapibus quis nec sem. Ut cursus,
          eros quis sollicitudin dapibus, erat purus sodales eros, in imperdiet libero
          leo nec dui. Integer pretium scelerisque ipsum, vitae congue felis.
        </p>
        <h3>Heading 3</h3>
        <p>
          Aenean rutrum sollicitudin lectus, vitae fringilla felis viverra nec.
          Donec congue posuere nulla egestas molestie. Interdum et malesuada
          fames ac ante ipsum primis in faucibus. Curabitur <mark
            >mark bibendum, turpis at vehicula vulputate</mark
          >, nulla lorem luctus ante, nec tincidunt lorem lectus et risus.
          Praesent nulla ligula, ultrices id rhoncus quis, interdum in ante.
          Cras faucibus neque odio, vel congue nulla scelerisque non.
          Pellentesque porttitor sed eros <a href="#" class="danger"
            >danger link</a
          > ut scelerisque. Nam urna nunc, dictum ut aliquet in, feugiat at arcu.
          Aenean in maximus felis. Mauris fermentum dolor iaculis, molestie augue
          id, molestie mauris. Aliquam in euismod lorem, sed feugiat ex. Pellentesque
          hendrerit enim vel orci viverra ultrices. Fusce auctor mollis fermentum.
          Nunc <a href="#" class="success">success link</a> elementum vel lacus eu
          feugiat. Aliquam id congue nisi. Duis non malesuada est, at feugiat leo.
        </p>
        <blockquote>
          "A blockquote with a cite."
          <cite>— someone</cite>
        </blockquote>
        <p><kbd>Ctrl</kbd> + <kbd>K</kbd></p>
        <hr />
      </div>
    </section>

    <section class="card">
      <h2 class="card-title">Buttons</h2>
      <div role="group" class="kitchen-sink-row">
        <button>Default</button>
        <button class="success">Success</button>
        <button class="contrast">Contrast</button>
        <button class="danger">Danger</button>
        <button class="warning">Warning</button>
        <button class="ghost">Ghost</button>
        <button class="reset">Reset</button>
        <button disabled>Disabled</button>
      </div>
    </section>

    <section class="card">
      <h2 class="card-title">Form</h2>
      <form class="kitchen-sink-form">
        <label>
          Text input
          <input type="text" placeholder="Type here" />
        </label>
        <label>
          Valid input
          <input type="text" aria-invalid="false" value="Looks good" />
        </label>
        <label>
          Invalid input
          <input type="text" aria-invalid="true" value="Something's off" />
        </label>
        <label>
          Textarea
          <textarea placeholder="Type something..."></textarea>
        </label>

        <label>
          Range
          <input type="range" />
        </label>
        <label><input type="checkbox" checked /> Checkbox</label>
        <label><input type="checkbox" role="switch" checked /> Switch</label>
        <label><input type="radio" name="ks-radio" checked /> Radio A</label>
        <label><input type="radio" name="ks-radio" /> Radio B</label>
        <label>
          File
          <input type="file" />
        </label>
        <label>
          Progress
          <progress value="60" max="100"></progress>
        </label>
      </form>
    </section>

    <section class="card">
      <h2 class="card-title">Table</h2>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Role</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Ada</td>
            <td>Admin</td>
            <td>Active</td>
          </tr>
          <tr>
            <td>Grace</td>
            <td>Editor</td>
            <td>Pending</td>
          </tr>
        </tbody>
      </table>
    </section>
  </div>
</Layout>
