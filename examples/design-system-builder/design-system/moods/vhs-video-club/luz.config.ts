import type { LuzConfig } from "../../../../../src/luz";

/**
 * Mood: 80s retro VHS video-club / Blockbuster-rental vibes, for a
 * movie-night meetup app (friends watch together, share comments and
 * moments). Deep video-store blue + tape-label gold, dark mode (CRT/late
 * -night rental aisle), condensed marquee headings, CRT monospace for
 * timestamps/comments.
 */
export const config: LuzConfig = {
  primary: "#1F4FD8",
  secondary: "#FFC72C",
  mode: "dark",
  font: `"Space Grotesk", sans-serif`,
  "font-headings": `"Bebas Neue", sans-serif`,
  "font-monospace": `"VT323", monospace`,
  power: 1.5,
};
