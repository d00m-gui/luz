//import { Button, type ButtonProps } from "@base-ui/react/button";

import { Avatar, type AvatarRootProps } from "@base-ui/react";

export function LuiAvatar(props: AvatarRootProps): React.ReactNode {
	return <Avatar.Root {...props} />;
}
